## i want to make 2 sperate pages instead of 1, 1 for worker it should be simple, stripped from all that features only has simple interface that notifies worker that there is a failure and makes the worker enter location and cause when it is finished

- So to be more informative the admin page should have all that logic implemented with the ability to edit stops log and also it should be in desc order

- while the worker only has that header of time and machine status and simple interface to enter location and cause of filure when it happens
